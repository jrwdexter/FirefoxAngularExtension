(function AngularListeners(){
    // 'use strict';
    
    /**
     * A base type for angular listeners, which will allow us to type coerce them.
     * Any additional functionality required by all listeners can be placed here.
     * @class ngListener
     * @constructor
     */
    var ngListener = function ngListener() {};

    exports.ngListener = ngListener;
})();